[package]
name = "nexa-dev-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
nexa-dev-ir = { path = "../nexa-dev-ir" }

[dev-dependencies]
nexa-ir = { path = "../nexa-ir" }
