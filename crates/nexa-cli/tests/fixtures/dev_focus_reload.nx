app FocusReloadSmoke {
    state email: String = ""
    state shouldFocus: Bool = true

    body {
        KeyboardAware(dismiss: Interactive) {
            Column {
                Text("Focus Ready")
                TextInput(value: email, placeholder: "Email", focused: shouldFocus)
                Text("Focus footer")
            }
        }
    }
}
