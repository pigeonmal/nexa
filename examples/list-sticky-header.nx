app FastListStickyHeaderDemo {
    state rows: Array<String> = ["First", "Second", "Third", "Fourth"]

    body {
        FastList(rows, axis: Vertical, key: .self, rowHeight: 52) { row, index in
            Text("\(index): \(row)")
        }.stickyHeader {
            Text("Items")
        }
    }
}
