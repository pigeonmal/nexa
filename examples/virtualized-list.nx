app VirtualizedListDemo {
    state rowCount: Int32 = 100000

    body {
        FastList(count: rowCount) { row in
            Row(spacing: 8) {
                Text(row)
                Text("rows")
            }
        }
    }
}
