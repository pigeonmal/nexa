app HorizontalListDemo {
    let products: Array<String> = ["Nexa", "SwiftUI", "Compose"]

    body {
        FastList(
            items: products,
            axis: Horizontal,
            index: position,
            item: product,
            key: product
        ) {
            Stack(minWidth: 140, minHeight: 72, padding: 12, background: "#F3F4F6") {
                Text(position)
                Text(product)
            }
        }
    }
}
