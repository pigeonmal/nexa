app CollectionListDemo {
    let products: Array<String> = ["Nexa", "SwiftUI", "Compose"]

    body {
        FastList(products, key: .self) { product, position in
            Row(spacing: 8, alignment: Center) {
                Text(position)
                Text(product)
            }
        }
    }
}
