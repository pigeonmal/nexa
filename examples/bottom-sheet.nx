app BottomSheetExample {
    state isOpen: Bool = false

    body {
        Column(spacing: 8) {
            Button("Open") {
                isOpen = true
            }
            BottomSheet(isPresented: isOpen) {
                Text("Sheet content")
                Button("Close") {
                    isOpen = false
                }
            }
        }
    }
}
