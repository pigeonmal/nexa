app TextInputSubmit {
    state query: String = ""
    state submitted: Bool = false

    body {
        Column(spacing: 12, padding: 20) {
            TextInput(value: query, placeholder: "Search", keyboard: Text) {
                submitted = true
            }
            if submitted {
                Text("Submitted: \(query)")
            }
        }
    }
}
