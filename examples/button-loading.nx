app ButtonLoading {
    state loading: Bool = false

    body {
        Column(spacing: 12) {
            Text("Native loading button")
            Button("Save", icon: "checkmark", loading: loading) {
                loading = true
            }
        }
    }
}
