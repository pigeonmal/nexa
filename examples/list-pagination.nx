app ListPaginationDemo {
    state rows: Array<String> = ["First", "Second"]

    body {
        FastList(rows, key: .self, rowHeight: 56) { row, index in
            Text(row)
        }.onEndReached {
            rows.append("More")
        }
    }
}
