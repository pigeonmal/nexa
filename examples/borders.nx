app Borders {
    body {
        Column(
            spacing: 12,
            padding: 20,
            background: "#F4F5F7",
            cornerRadius: 16,
            borderColor: "#D1D5DB",
            borderWidth: 1
        ) {
            Text("Native border")
            Text("The compiler emits a platform-native border modifier.")
        }
    }
}
